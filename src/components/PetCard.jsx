import styles from './PetCard.module.css';


export default function PetCard({ children, ...props }) {
  return (
    <div className="NotImplemented" {...props}>
      Not Implemented: PetCard {children}
    </div>
  );
}
